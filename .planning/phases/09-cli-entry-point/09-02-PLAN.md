---
phase: 09-cli-entry-point
plan: "02"
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - src/alteryx_diff/renderers/html_renderer.py
autonomous: true
requirements:
  - CLI-04

must_haves:
  truths:
    - "HTML report contains a collapsed governance footer with File A path, SHA-256 A, File B path, SHA-256 B, and generated timestamp"
    - "The footer is collapsed by default (HTML `<details>`/`<summary>`) — auditors expand manually, casual readers unaffected"
    - "SHA-256 values are the full 64-character hex string (ALCOA+ requirement)"
    - "Governance metadata appears only in the report file, not in terminal output"
    - "`HTMLRenderer.render()` accepts a `metadata` dict parameter and embeds it in the template"
  artifacts:
    - path: "src/alteryx_diff/renderers/html_renderer.py"
      provides: "Updated _TEMPLATE with <details> governance footer; render() accepts metadata: dict | None"
  key_links:
    - from: "src/alteryx_diff/cli.py"
      to: "src/alteryx_diff/renderers/html_renderer.py"
      via: "HTMLRenderer().render(result, ..., metadata=metadata)"
      pattern: "HTMLRenderer.*render.*metadata"
    - from: "src/alteryx_diff/renderers/html_renderer.py"
      to: "_TEMPLATE governance section"
      via: "Jinja2 {{ metadata.sha256_a }} etc. inside <details> block"
      pattern: "details.*governance"
---

<objective>
Add a collapsible governance metadata footer to the HTML report template (CLI-04). Extend `HTMLRenderer.render()` to accept a `metadata` dict and render the ALCOA+ footer section inline. When `metadata` is `None` (existing callers, test suite), the footer is omitted — zero regression risk.

Purpose: ALCOA+ audit compliance — source file paths and SHA-256 digests embedded in every generated HTML report.
Output: Updated `src/alteryx_diff/renderers/html_renderer.py`.
</objective>

<execution_context>
@/Users/laxmikantmukkawar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laxmikantmukkawar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-cli-entry-point/09-CONTEXT.md
@.planning/phases/09-cli-entry-point/09-RESEARCH.md
@.planning/phases/09-cli-entry-point/09-01-SUMMARY.md

<interfaces>
<!-- Current HTMLRenderer.render() signature — extend this, do not replace -->

From src/alteryx_diff/renderers/html_renderer.py:
```python
class HTMLRenderer:
    def render(
        self,
        result: DiffResult,
        file_a: str = "workflow_a.yxmd",
        file_b: str = "workflow_b.yxmd",
        *,
        graph_html: str = "",
    ) -> str: ...
```

New signature after this plan:
```python
def render(
    self,
    result: DiffResult,
    file_a: str = "workflow_a.yxmd",
    file_b: str = "workflow_b.yxmd",
    *,
    graph_html: str = "",
    metadata: dict[str, Any] | None = None,   # NEW — None = no footer (backward compat)
) -> str: ...
```

Governance metadata dict structure (from cli.py _build_governance_metadata):
```python
{
    "file_a": str,         # absolute path
    "file_b": str,         # absolute path
    "sha256_a": str,       # 64-char hex
    "sha256_b": str,       # 64-char hex
    "generated_at": str,   # ISO-8601
}
```

From CONTEXT.md (locked decisions):
- Footer collapsed by default — HTML `<details>`/`<summary>` element (no JS required)
- Full 64-char SHA-256 — do not truncate
- Governance metadata appears ONLY in report file, NOT in terminal output
- Minimum fields only: file paths, SHA-256 hashes, generation timestamp
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend HTMLRenderer with governance metadata footer</name>
  <files>
    src/alteryx_diff/renderers/html_renderer.py
  </files>
  <action>
Read the current `html_renderer.py` first (it has `# ruff: noqa: E501` at top — preserve this).

**Changes to `_TEMPLATE` string:**

Insert the following block BEFORE the closing `</div>` of `class="container"` AND after the `{{ graph_html | safe }}` line (i.e., just before `</div>\n</body>\n</html>`):

```html
{% if metadata %}
<details id="governance" style="margin-top:32px;border-top:1px solid #dee2e6;padding-top:12px;">
  <summary style="cursor:pointer;color:#888;font-size:0.85em;padding:4px 0;user-select:none;">
    Governance Metadata (ALCOA+)
  </summary>
  <div style="font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;font-size:0.82em;padding:8px 0;color:#555;line-height:1.8;">
    <div><strong>File A:</strong> {{ metadata.file_a }}</div>
    <div><strong>SHA-256 A:</strong> {{ metadata.sha256_a }}</div>
    <div><strong>File B:</strong> {{ metadata.file_b }}</div>
    <div><strong>SHA-256 B:</strong> {{ metadata.sha256_b }}</div>
    <div><strong>Generated:</strong> {{ metadata.generated_at }}</div>
  </div>
</details>
{% endif %}
```

**Changes to `HTMLRenderer.render()` method:**

1. Add `metadata: dict[str, Any] | None = None` as a keyword-only parameter.
2. Pass `metadata=metadata` to `template.render(...)` so the Jinja2 template can access it.
3. The `{% if metadata %}` guard in the template handles `None` — no footer rendered when callers omit `metadata`.

The existing 7 `test_html_renderer.py` tests all call `render()` without `metadata` — they will continue to pass unchanged because `metadata=None` is the default and the `{% if metadata %}` block is skipped.

IMPORTANT: The `_TEMPLATE` string uses Jinja2 with `autoescape=True`. The `metadata` dict values are strings (paths, hex digits, ISO timestamp) — auto-escaping is safe and correct for these. Do NOT use `| safe` on metadata values (they are user-controlled file paths and could contain HTML chars).

After editing, verify no ruff violations:
- `from typing import Any` is already imported in html_renderer.py
- `dict[str, Any] | None` is valid Python 3.10+ syntax (project requires >=3.11)
- `from __future__ import annotations` is at the top — ensures forward-ref compatibility
  </action>
  <verify>
    <automated>cd /Users/laxmikantmukkawar/Documents/Projects/alteryx_diff && python -m pytest tests/test_html_renderer.py -x -q 2>&1 | tail -5 && python -c "
from alteryx_diff.renderers import HTMLRenderer
from alteryx_diff.models import DiffResult
result = DiffResult(added_nodes=(), removed_nodes=(), modified_nodes=(), edge_diffs=())
meta = {'file_a': '/tmp/a.yxmd', 'file_b': '/tmp/b.yxmd', 'sha256_a': 'a'*64, 'sha256_b': 'b'*64, 'generated_at': '2026-03-06T00:00:00+00:00'}
html = HTMLRenderer().render(result, metadata=meta)
assert 'governance' in html
assert 'ALCOA' in html
assert 'a'*64 in html
print('governance footer ok')
html_no_meta = HTMLRenderer().render(result)
assert 'governance' not in html_no_meta
print('no-metadata path ok')
"</automated>
  </verify>
  <done>All 7 existing `test_html_renderer.py` tests pass. HTML output with `metadata=` includes a `<details id="governance">` block containing all 5 fields with full 64-char SHA-256 values. HTML output without `metadata` has no governance block.</done>
</task>

</tasks>

<verification>
Full suite gate confirming zero regressions:
```bash
cd /Users/laxmikantmukkawar/Documents/Projects/alteryx_diff && python -m pytest tests/ -q 2>&1 | tail -5
```
</verification>

<success_criteria>
- `HTMLRenderer.render()` accepts `metadata: dict[str, Any] | None = None` keyword argument
- `_TEMPLATE` contains `{% if metadata %}...{% endif %}` governance footer using `<details>`/`<summary>`
- Footer includes file paths, SHA-256 hashes (full 64-char), and generation timestamp
- Existing 7 `test_html_renderer.py` tests pass unchanged
- No ruff lint errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/09-cli-entry-point/09-02-SUMMARY.md`
</output>
